{
  "name": "try",
  "version": "1.0.0",
  "description": "123123",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "changelog": "conventional-changelog -p angular -i CHANGELOG.md -w -r 0",
    "detaillog": "git log --pretty=oneline",
    "modules": "commitizen init cz-conventional-changelog --save --save-exact"
  },
  "author": "wr",
  "license": "ISC",
  "devDependencies": {
    "cz-conventional-changelog": "^2.1.0"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  },
  "dependencies": {
    "lottie-web": "^5.2.1"
  }
}
